import { redirect } from "next/navigation";

/* Раньше здесь был список из трёх карточек «выберите калькулятор».
   Он ничего не считал и стоил лишнего клика: человек, пришедший считать
   платёж, сначала попадал на страницу-оглавление. Переключатель калькуляторов
   теперь стоит в layout, поэтому оглавление стало не нужно, и /calculators
   ведёт сразу на самый частый из них. */
export default function CalculatorsIndex() {
  redirect("/az/calculators/consumer-loan");
}
